import { Sparkles } from "lucide-react";

const HeroTag = () => (
  <div className="inline-flex items-center space-x-2 bg-white/80 backdrop-blur-sm rounded-full px-4 py-2 text-sm font-medium text-purple-700 border border-purple-200">
    <Sparkles className="h-4 w-4" />
    <span>Reimagining Customer Experience</span>
  </div>
);

export default HeroTag;
